package hw1;

public class NegadecimalNumber {

	// reads 2 negadecimal numbers and an operator and proceeds in performing the
	// calculations.

	private int negadecimal1;
	private int negadecimal2;
	private char operator;

public NegadecimalNumber(String number) { 
	
	for (int i=0; i<number.length(); i++) {
		for (int j=0; j<=9; j++) {
		if (number.charAt(i) != j || number.isEmpty()) {
			System.out.print("IllegalArgumentException");
		}
		}
	}
	else { int negadecimal = Integer.parseInt(number);
 
	}

}

public NegadecimalNumber(int n) {

	
	
	//TestNegadecimalCalculator();

public int toDecimal() {
	int negadecimal = new NegadecimalNumber();
	for (int i=0; i<negadecimal.length; i++) {
		
	}
	
}
	
	
	
	
	
	
	
}

}
